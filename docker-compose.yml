version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./etc/php/Dockerfile
    volumes:
      - ./:/var/www
    ports:
      - "8001:8000"
  app_supervisor:
    build:
      context: .
      dockerfile: ./etc/supervisor/Dockerfile
    volumes:
      - ./:/var/www
